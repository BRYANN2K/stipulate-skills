---
description: Reconcile an applied change with every acceptance criterion using fresh, inspectable evidence.
---

Load the installed `stip-check` skill using the `skill` tool and follow its instructions in the current project. Resolve scripts and references from the loaded skill directory. If the skill is unavailable, report the missing installation instead of inventing its procedure.

User request:
$ARGUMENTS
