---
description: Implement an approved workflow change and continue through relevant tests and corrections within its contract.
---

Load the installed `stip-apply` skill using the `skill` tool and follow its instructions in the current project. Resolve scripts and references from the loaded skill directory. If the skill is unavailable, report the missing installation instead of inventing its procedure.

User request:
$ARGUMENTS
