---
description: Create or update documentation affected by a checked change, grounded in the implementation and intended readers.
---

Load the installed `stip-docs` skill using the `skill` tool and follow its instructions in the current project. Resolve scripts and references from the loaded skill directory. If the skill is unavailable, report the missing installation instead of inventing its procedure.

User request:
$ARGUMENTS
