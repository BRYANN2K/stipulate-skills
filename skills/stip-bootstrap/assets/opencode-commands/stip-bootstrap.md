---
description: Adopt Stip in a new or existing repository, preserving project conventions and assessing applicable domains.
---

Load the installed `stip-bootstrap` skill using the `skill` tool and follow its instructions in the current project. Resolve scripts and references from the loaded skill directory. If the skill is unavailable, report the missing installation instead of inventing its procedure.

User request:
$ARGUMENTS
