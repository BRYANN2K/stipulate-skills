---
description: Turn an explored change into a reviewable specification and record explicit user approval of its current version.
---

Load the installed `stip-validate` skill using the `skill` tool and follow its instructions in the current project. Resolve scripts and references from the loaded skill directory. If the skill is unavailable, report the missing installation instead of inventing its procedure.

User request:
$ARGUMENTS
