{
  "schema_version": 1,
  "extension": "cli-tooling",
  "purpose": "Editorial cases for human or agent evaluation; structural tests do not establish these decisions.",
  "cases": [
    {
      "id": "relevant",
      "prompt": "add a JSON command that reports errors and interruptions cleanly.",
      "expected": "select",
      "reason": "Build a stable command-line interface for people and automation."
    },
    {
      "id": "irrelevant",
      "prompt": "change a graphical component without affecting commands or automation.",
      "expected": "do-not-select",
      "reason": "No responsibility of this domain is affected."
    },
    {
      "id": "existing",
      "prompt": "add a JSON command that reports errors and interruptions cleanly. The project already has an implementation and evidence of this capability, but no dedicated document.",
      "expected": "inspect-and-reuse",
      "reason": "Inspect evidence, classify coverage, and address only gaps; missing documentation does not mean the domain practice is absent."
    },
    {
      "id": "insufficient",
      "prompt": "The command works in a terminal but prints a banner to stdout, breaking JSON in a pipeline.",
      "expected": "failed-or-unverified",
      "reason": "Compare against the approved criterion outcome; producing a file does not establish that the outcome was achieved."
    }
  ]
}
