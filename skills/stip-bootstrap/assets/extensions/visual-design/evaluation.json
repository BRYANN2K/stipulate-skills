{
  "schema_version": 1,
  "extension": "visual-design",
  "purpose": "Editorial cases for human or agent evaluation; structural tests do not establish these decisions.",
  "cases": [
    {
      "id": "relevant",
      "prompt": "fix unreadable hierarchy and table states on small screens.",
      "expected": "select",
      "reason": "Define appropriate hierarchy, readability, colors, typography, spacing, and visual states."
    },
    {
      "id": "irrelevant",
      "prompt": "change a server worker without affecting visual surfaces.",
      "expected": "do-not-select",
      "reason": "No responsibility of this domain is affected."
    },
    {
      "id": "existing",
      "prompt": "fix unreadable hierarchy and table states on small screens. The project already has an implementation and evidence of this capability, but no dedicated document.",
      "expected": "inspect-and-reuse",
      "reason": "Inspect evidence, classify coverage, and address only gaps; missing documentation does not mean the domain practice is absent."
    },
    {
      "id": "insufficient",
      "prompt": "An attractive mockup is delivered, but real text overflows and a state relies on color alone.",
      "expected": "failed-or-unverified",
      "reason": "Compare against the approved criterion outcome; producing a file does not establish that the outcome was achieved."
    }
  ]
}
