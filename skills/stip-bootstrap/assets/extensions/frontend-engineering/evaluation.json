{
  "schema_version": 1,
  "extension": "frontend-engineering",
  "purpose": "Editorial cases for human or agent evaluation; structural tests do not establish these decisions.",
  "cases": [
    {
      "id": "relevant",
      "prompt": "implement web search with loading, errors, keyboard navigation, and asynchronous results.",
      "expected": "select",
      "reason": "Build rendered web behavior, including states, data, accessibility, and performance."
    },
    {
      "id": "irrelevant",
      "prompt": "change only infrastructure without affecting frontend behavior.",
      "expected": "do-not-select",
      "reason": "No responsibility of this domain is affected."
    },
    {
      "id": "existing",
      "prompt": "implement web search with loading, errors, keyboard navigation, and asynchronous results. The project already has an implementation and evidence of this capability, but no dedicated document.",
      "expected": "inspect-and-reuse",
      "reason": "Inspect evidence, classify coverage, and address only gaps; missing documentation does not mean the domain practice is absent."
    },
    {
      "id": "insufficient",
      "prompt": "The build succeeds, but the browser shows an empty list because a dependency service is unavailable.",
      "expected": "failed-or-unverified",
      "reason": "Compare against the approved criterion outcome; producing a file does not establish that the outcome was achieved."
    }
  ]
}
