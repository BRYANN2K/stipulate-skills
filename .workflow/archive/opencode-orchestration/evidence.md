# Evidence

Subject: `39dc82c765b129cb9624435e055f92246f2252f0c0708263958ee708d68af0cb`

- AC-1: passed — packages/opencode/src/index.ts calls the native subagent tool and transforms native child agents; no Codex/Claude executable is launched. docs/verification.md records three distinct live child session IDs from OpenCode beta-19296.
- AC-2: passed — Backend and independent Verification inherited the live coordinator model. routing.test.mjs covers inherited model/settings and OpenCode serialized default variants; the explicit research helper also inherited its model with a phase effort override.
- AC-3: passed — routing.mjs and configure_agents.py support role, phase and phase-role profiles. Native configuration and routing tests cover precedence and reserved Discovery roles. The real settings menu showed the role profiles; Discovery stays with the main conversation.
- AC-4: passed — settings-ui.test.mjs verifies every offered combination resolves while preserving the other setting. routing tests reject unsupported combinations and unavailable models. In the live menu Documentation selected low and the actual child reported #low; unsupported Fast was absent. Paid live Fast calls were not exercised.
- AC-5: passed — The inspected native /stip-settings menu uses OpenCode catalog entries and contains no runtime picker. profile-options.mjs and routing tests exclude unusable models and reject unknown variants.
- AC-6: passed — tests/test_native_agents.py passed native Codex TOML, Claude Markdown and OpenCode projection, ownership, collision, capability and rollback scenarios. These are file-generation tests; Codex/Claude runtimes were not launched.
- AC-7: passed — All seven skills load relevant orchestration references and enabled domain material. The real add-greeting fixture selected backend-engineering only. Extension/package validation passed for all 28 extensions; the fixture did not require unrelated design workers.
- AC-8: passed — The live coordinator delegated implementation, verification and documentation, inspected results and explicitly accepted each contribution. The outer authorized harness recorded fresh checks and archive. Coordinator tests reject recursive worker delegation; workers cannot approve contracts or finalize lifecycle state.
- AC-9: passed — coordinator.test.mjs and research.test.mjs cover concurrent reservation, restart deduplication, permission waits, missing models, uncertain launch and confirmed/unconfirmed cancellation. Earlier native interruption/retry qualification is recorded in docs/verification.md; uncertain states remain inspectable rather than successful.
- AC-10: passed — Actual Ghostty screenshots showed the lifecycle below MCP, waiting review, accepted results, separate research findings and all seven archived stages. Single clicks on Settings and Documentation opened persistent dialogs after release. Routing tests verify stale-approval and phase projection; native-host failure cases have controlled coordinator tests.
- AC-11: passed — tests/test_orchestration_workflow.py and tests/test_workflow.py passed contract digest invalidation, migration, stale registry, fresh source evidence, post-check documentation boundaries and archive consistency cases. The engine rechecks approval and subject digests at each finalization gate.
- AC-12: passed — Installer and native configuration tests passed collision, customization, symlink and rollback preservation. Packed npx fresh installation and update preserved real extension/model settings. Legacy v1 lifecycle tests passed; no migration is implicit.
- AC-13: passed — docs/verification.md records the isolated CLI add-greeting v2 change, backend extension, three real native child sessions, independent five-case Python verification, guide example execution and scoped archive commit d93088b459cde6dc063454a44c9cae5d990abc3f. Subsequent installer/settings housekeeping was committed separately.
- AC-14: passed — Research is invoked explicitly by stip_research; lifecycle discovery does not automatically dispatch helpers. Native ses_f77c6a4b1ffeMANE0FVg4Ip9T5 returned bounded read-only findings with actual #minimal effort to the coordinator. The entire source snapshot remained identical and no approval or implementation contribution was created.
- AC-15: passed — The real sidebar showed returned contributions awaiting review before acceptance. coordinator.test.mjs covers pending acceptance and scoped correction of an accepted implementation without automatic check success; engine tests reject missing or superseded attempts.
- AC-16: passed — Verification used native ses_f77ddafb2ffefR1GSz42cVNIGU, separate from implementer ses_f77e04fb5ffeMGvt9BllTYJiyL. Coordinator tests reject reuse of implementer sessions for verification. Workflow tests require renewed evidence for source changes and restrict post-check docs changes to declared documentation paths.
- AC-17: passed — Earlier native interruption/retry qualification reused its worker session; coordinator tests cover scoped correction, acceptance invalidation and separation of unrelated/verification tasks. The complete fixture used three distinct native sessions for implementation, verification and documentation.
- AC-18: passed — Plan tests passed DAG validation, criterion coverage, path traversal/symlink rejection and ordered ownership. Coordinator tests verify accepted dependencies and project-wide worker/writer limits. Scoped archive and preservation tests passed. Shared-checkout permission controls are not an OS sandbox, as documented.
