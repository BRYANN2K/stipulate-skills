# Creative brief

> Optional working artifact. Persist only when requested, needed across runs/agents, or required by the project. Delete sections that do not apply; do not fill them with generic prose.

## Scope

- Surface / mode:
- Surface class by affected route: `PUBLIC_CONTENT_NAVIGATION_SITE` / `CONVERSION_LANDING` / `TASK_DATA_UI`
- Primary job to preserve: find/browse/understand/navigate | evaluate/convert | operate/decide/mutate
- Bounded, new surface, or new identity/pattern:
- Requested local build scope:
- Decision owner: human | delegated | not-applicable
- Authority reference:

## Product truth

- Product/mechanism:
- Audience and job/decision:
- Desired action/success:
- Evidence available:
- Unknowns that can change the result:
- Existing brand/system/assets to preserve:

## Constraints and boundaries

- Repository/runtime constraints:
- Content/data/permission constraints:
- Claims requiring a ledger:
- Not authorized: dependencies | live/destructive effects | publication/deployment | other

## Storyboard

| Frame/beat | User question or action | Content/data/proof | State/permission | Transition/next action |
|---|---|---|---|---|
| 1 |  |  |  |  |

## Success criteria

- Product/task clarity:
- Behavioral outcome:
- Decision- or claim-changing viewports/states required:
- Material no-go:
## Conditional real-time scene contract

> Omit unless WebGL/WebGPU/3D/shaders materially serve the selected concept.

- Canvas role: decorative | informative | interactive | essential
- Product beat and why CSS/SVG/image/video is insufficient:
- Remove-canvas result; semantic DOM/poster parity:
- Renderer/backend and exact installed versions:
- Document/DOM, canvas, scroll, pointer/focus and resource ownership:
- State machine: static → loading → ready/running ↔ paused → lost/fallback → disposed
- Native scroll beat → DOM proof → scene/camera state → static/reduced equivalent:
- No-GPU, init/asset/shader failure, context/device loss, mobile/touch, reduced motion/data:
- Project/device-specific JS/assets, DPR/quality, frame/GPU/main-thread and Web Vitals budgets:
- Offscreen/hidden pause, resize/reflow, mount/unmount and cleanup owners:
- Deterministic evidence inputs: viewport, physical canvas/DPR, theme, seed, time/frame, scroll, pointer, warmups, readiness:
- Asset/model/HDRI/texture/font provenance and licence:
