# Reference Ledger and Direction Set

> Optional handoff/re-entry scaffold, not an admission ticket. Inline citations and a single direction are valid. Persist only when requested, required by the project, actual reuse/attribution must survive, or the decision crosses runs/agents. Duplicate the direction section only for a material trade-off; every alternative in an open visual decision needs a viewable render.

## Working contract

- Surface/route:
- Mode: `TARGETED_DIRECTION` / `OPEN_EXPLORATION` / `TASK_PATTERN` / `INHERIT_SYSTEM`
- Decision mode: `human` / `delegated` / `not-applicable`
- Product/task decision the visuals must support:
- Copy/content and optional claims source:
- Existing system/brand constraints:
- Accessibility/platform/stack constraints:
- Decision-relevant viewport/state:

## Fair-comparison constants — only when alternatives exist

- Product facts/caveats/proof inventory shared fairly:
- Primary action/task and success consequence:
- Representative content/data/state:
- Viewport/device/content assumptions:
- Synthetic or placeholder material and visible labels:
- Constraints that may not change between candidates:

## Source — `<ID>`

- Exact primary URL:
- Creator/maintainer, if known:
- Artifact/viewport/state inspected and date:
- Role:
- Exact observation:
- Interaction evidence actually observed (live path/recording/trace/source), or `still-only — interaction unobserved`:
- Adaptation principle tied to this product:
- Licence/reuse status and terms locator:
- Originality decision independent of licence:
- No-copy boundary, including recognizable composition/sequence/combined expression:
- Reuse decision: `principle-only` / `licensed code` / `licensed asset` / `owned` / `public-domain` / `excluded`

## Direction — `<ID>`

- Serves copy route or governing content:
- Thesis:
- Product/subject-world signature and function:
- Source adaptations used:
- Layout/topology and attention path:
- Type roles, scale, measure, and reading behavior:
- Density/rhythm and palette semantics:
- Asset/imagery/data language and provenance:
- Motion posture and reduced equivalent:
- Viewable prototype/frame/annotated locator:
- Interaction claims and actual observation/prototype evidence; still-only boundaries:
- Contrasting viewport/state evidence only if decision-relevant:
- Recognizable-composition check independent of source licence:
- Honest trade-off and concept-specific no-go:
- Accessibility/responsive/implementation risk:
- Conditional real-time need, remove-canvas result, fallback/reduced mode, renderer/lifecycle risk:

## Conditional real-time scene comparison

> Complete only for a candidate admitted by explicit request or a named product need. Keep framework APIs in official relevant skills/docs.

- Canvas role and product beat:
- Why CSS/SVG/image/video is insufficient:
- Remove-canvas test — DOM proposition/proof/order/controls/CTA preserved:
- Spatial value specifically lost without canvas:
- Semantic DOM/poster and accessible/reduced equivalent:
- Document scroll, canvas/renderer, pointer/focus/events/resource owners:
- Existing renderer versions or separate dependency authorization needed:
- Applicable loading/asset/shader failure, no-GPU, context/device loss, reduced motion/data, mobile/touch behavior:
- Project/device-specific budgets and evidence method:
- Offscreen/hidden pause, resize/reflow, navigation/unmount and cleanup owner:
- Deterministic keyframes/states needed by this decision:
- Asset/model/HDRI/texture/font sources and licence IDs:

## Inherit-system decision

- Governing system and exact locator:
- Why variants/research would be artificial:
- Existing type/spacing/color/component/state rules inherited:
- Affected placement and annotated locator/sketch, if needed:
- Necessary local exception and rationale:

## Comparison and selection — only when alternatives exist

| Candidate | Product/task fit | Content/proof fit | Accessibility/responsive risk | Implementation cost/risk | Originality without copied expression | Key trade-off |
|---|---|---|---|---|---|---|
|  | | | | | | |

- Recommendation or direct direction:
- Rationale grounded in evidence:
- Selected direction: `<ID>` / inherited / pending human
- Decision owner/reference:
- Accepted trade-off:
- Open risk or unresolved check:

## Selected-direction build brief

- Thesis and product/subject-world signature:
- Composition/topology and responsive priority:
- Type/density/palette semantics:
- Asset provenance and imagery/data language:
- Motion intent and reduced equivalent:
- Representative states/content constraints:
- Project-specific no-go:
- Prototype/frame evidence paths:
- Full design system deferred or not needed:
- Effects not proven: runtime / accessibility / performance / dependency / deployment / publication:
