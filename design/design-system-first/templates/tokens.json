{
  "$description": "Optional DTCG-shaped interchange template. Use this shape only when a named external consumer declares DTCG support or the requested deliverable requires it; otherwise preserve the repository-native token format. Declare the canonical-to-generated direction and generated owner, remove unused examples, and never mirror runtime values into a competing source of truth. Validate effective types, aliases, inheritance and cycles only where used; JSON/schema success alone does not prove consumer resolution or runtime integration.",
  "color": {
    "surface": {
      "default": {
        "$type": "color",
        "$value": "{{SEMANTIC_SURFACE_VALUE}}",
        "$description": "Primary shared surface role."
      }
    },
    "text": {
      "primary": {
        "$type": "color",
        "$value": "{{SEMANTIC_TEXT_VALUE}}",
        "$description": "Primary text role on the default surface."
      }
    },
    "focus": {
      "ring": {
        "$type": "color",
        "$value": "{{SEMANTIC_FOCUS_VALUE}}",
        "$description": "Keyboard focus indicator role; preserve non-color visibility and contrast requirements."
      }
    }
  },
  "space": {
    "related": {
      "$type": "dimension",
      "$value": "{{RELATED_CONTENT_SPACING}}",
      "$description": "Spacing relationship for semantically related elements."
    },
    "section": {
      "$type": "dimension",
      "$value": "{{SHARED_SECTION_SPACING}}",
      "$description": "Shared section separation only where the role is consumed."
    }
  }
}
